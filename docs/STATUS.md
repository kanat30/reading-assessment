# Status — FluencyScope
_Last updated: 2026-07-14 by Claude Code (multi-passage dashboard grouping; stop-point robustness; async comprehension grading)_

**Stack:**
Next.js 16 (App Router) + React 19, shipped as a PWA (Serwist service worker) · Supabase (Postgres + RLS, Auth, Storage) · Deepgram `nova-3` ASR · Claude API (`@anthropic-ai/sdk`, model `claude-sonnet-4-6` via `lib/scoring/ai.ts`) · Tailwind CSS v4 + Base UI/shadcn + Framer Motion · wavesurfer.js · TypeScript · deployed on Vercel. Node 20+ (no `engines` field pinned).

**What's working / built:**
- Teacher auth (Supabase email/password) and dashboard with assessment/session lists, current-week default window, date filters, shareable assessment links, templates, numbered students.
- Student flow (no login): `read/[token]` → name entry → recording (MediaRecorder) → comprehension → done. **The read is a fixed 60-second timed sample: the app auto-stops and advances at the limit (no reliance on the student tapping stop), shown as a calm depleting bar with no ticking numbers and an "Almost done" cue in the final 10s; a manual stop remains for early finishers.** Upload retries with backoff and has offline recovery (auto-uploads when the connection returns); a failed upload keeps the recording so the student never re-reads.
- Scoring pipeline (`POST /api/score`): Deepgram `nova-3` (`filler_words`, passage `keyterm` prompting) → deterministic alignment/error classification → WCPM, accuracy, Hasbrouck–Tindal benchmark bands (BOY/MOY/EOY, median-of-3) → prosody (1–4) → error patterns → Claude teacher summary. Hardened: 25MB/10min caps, per-token rate limiting.
- **Not-reached handling (2026-07-11):** since the read is a fixed 60s sample, words past where the student stopped are excluded from scoring, error patterns, error counts, and the transcript (which collapses the unread remainder behind a "reached N of M words" marker) — they are no longer shown or counted as omission errors. Cutoff derived by `getLastReachedIndex()` (`lib/scoring/metrics.ts`). Migration 0019 applies the same cutoff to the override metric-recompute (fixes a latent bug where the first word-override recomputed accuracy over the whole passage). See DECISIONS 2026-07-11.
- **Stop-point robustness (2026-07-14):** fixed a case where a student stopping on a word that recurs later in the passage (e.g. the first "construction") had their stop point teleported to the later copy, marking the whole intervening paragraph as read-but-omitted and corrupting WCPM/accuracy/band. Two deterministic fixes: (A) the NW aligner (`alignment.ts`) now biases ties toward the *earliest* occurrence of a repeated word, keeping the read contiguous; (B) `getLastReachedIndex()` now returns the end of the leading contiguous read — it cuts at the first large silent gap followed by only a sparse stray tail, while leaving genuine mid-passage skips counted as omissions. Guarded by `scripts/validate-stop-point.ts` (3 scenarios, all pass). Score-time only, no migration. See DECISIONS 2026-07-14.
- **Comprehension submit no longer blocks on AI (2026-07-14):** the student's "Submit answers" was waiting 10–30s per passage for the synchronous Claude grading call — output only the teacher sees. `POST /api/comprehension` now persists the raw answers, returns immediately, and grades in the background via `waitUntil`; the report shows "Grading…" for the brief pending window. Student submit is now a fast DB write. No migration. See DECISIONS 2026-07-14.
- **AI features are actually live as of 2026-07-10.** Three of four Claude call sites (summary, prosody, question generation) had shipped with a nonexistent model ID and were silently falling back for weeks. All four now use `CLAUDE_MODEL` from `lib/scoring/ai.ts` (verified against the live API); fallbacks log `[AI-FALLBACK]` so silent degradation can't recur.
- Leveled passage library (~60 passages, levels 3–7, Forms A/B/C, 300+ words each) with comprehension questions.
- Teacher report (`report/[id]` + print view): synced transcript, waveform, prosody gauges, error patterns, AI summary with advisory-only disclaimer, word-level overrides (approve/reject/flag with metric recompute), comprehension regrade, teacher notes + review-status workflow. **Multi-passage display fix (2026-07-11):** dashboard, report, and print view now resolve each session's passage from `sessions.passage_id` via the library instead of the assessment's single legacy passage, so median-of-3 sessions show their real distinct titles/text and the transcript matches the AI summary. See DECISIONS 2026-07-11.
- **Multi-passage dashboard grouping + overall median report (2026-07-14):** a student's median-of-3 assessment now shows as one grouped dashboard row (student · Level N · K passages · median X WCPM, with a "R of K" badge and a K-segment progress indicator) instead of three separate readings. Expanding reveals a compact **overall median-of-3 report** (`components/report/GroupMedianReport.tsx`) — median WCPM + Hasbrouck–Tindal benchmark band (reusing `calculateMedianResult` + `MedianScoreSummary`, three passage scores shown with the median highlighted), plus median accuracy, typical prosody, aggregate comprehension, and an advisory note — followed by one expandable per-passage sub-report, each reusing the existing row so per-passage status/notes/delete/override still work. The overall report is derived entirely from the submitted passages' `scores_json`; partial reads (1–2 of 3) are flagged and computed honestly. Display-only (`app/dashboard/client.tsx` + the `passage_index`/`passage_ids`/`reading_level`/`assessment_period` fields added to the dashboard fetches), no migration, no new API, single-passage assessments unchanged. The standalone `/report/[id]` page still shows one passage at a time (reusing `GroupMedianReport` there is the natural next step). See DECISIONS 2026-07-14.
- **Compliance surface started:** public `/explainability` page (deterministic scoring vs AI use, teacher override rights, data handling, known ASR limits) linked from the report; advisory-only framing on AI outputs.
- **Endpoint authorization:** `/api/audio/[id]` (student audio = potential biometric) and `/api/comprehension/regrade` now require an authenticated teacher of the session's school; anonymous `/api/comprehension` (POST submit + new GET passage/questions lookup) is bounded by the assessment `share_token` + a 2-hour window and one-time grading. See DECISIONS 2026-07-10.
- **Report is teacher-only (2026-07-13, applied to live 2026-07-14):** removed all anonymous RLS read access to `sessions`, `session_events`, `comprehension_answers`, `students` (migration 0020). The comprehension flow's only anon session read moved to the bounded `GET /api/comprehension`; the student "view report" link was removed from the done page. Closes the gap where anyone with a session UUID could read the full report (WCPM/band/transcript/name) for 30 days — **verified live after apply: anon now reads 0 rows across all four tables** (service role still sees 34/2626/84/24). 0020 is **catalog-driven** (discovers the anon policies from `pg_policies` at apply time, not by assumed name) and self-verifying (aborts + rolls back atomically if any table would lose its school-scoped teacher policy or keep an anon read policy). Validated on a local Postgres repro before apply. **⚠️ Deploy coupling:** the matching code (`GET /api/comprehension` + comprehension page) is still uncommitted/undeployed — until it ships, production's old code reads `sessions` as anon and silently skips comprehension. See DECISIONS 2026-07-13.
- **Library-assessment creation fixed (2026-07-14):** creating any passage-library assessment (incl. the 3-passage median-of-3 flow) 400'd because `assessments.passage_id` was still `NOT NULL` from `0001` while the library path inserts `passage_id = NULL` (`0016` never relaxed it). Migration `0021` drops the `NOT NULL` and adds a stricter CHECK `assessments_has_passage_source` (`passage_id IS NOT NULL OR cardinality(passage_ids) > 0`) so a passage source is still guaranteed. **Applied to the live DB and verified** (`passage_id` no longer in the table's PostgREST `required` set). RLS/tenancy untouched. See DECISIONS 2026-07-14.
- Admin console + analytics; audio via `/api/audio/[id]`; maintenance `scripts/` incl. `validate-wer.ts`.
- 21 ordered SQL migrations, **all applied to the live DB**: 0001–0018 verified 2026-07-10; 0019 + 0020 + 0021 applied 2026-07-14 (0020 verified live via anon probe = 0 rows; 0021 verified via PostgREST `required` set; 0019 applied in the same push, not independently re-verified — it only affects the override-recompute path). 0017 fixed the override recalc (self-corrections count as correct; latest-override-wins) and revoked client-role EXECUTE on the internal helper — verified live: anon now gets 42501 where it previously executed; the backfill repaired session `43f1f6c4` exactly as predicted (WCPM 108→110, accuracy 96→98%). 0018 added the missing school check to `apply_session_override`. `npm run lint` (0 errors) and `npm run build` pass as of 2026-07-13.

**In progress:**
- **Migrations 0019–0021 are applied to live, but the matching app code is still uncommitted/undeployed** — a DB-ahead-of-code window. Most urgent: the teacher-only-report code (`GET /api/comprehension` + comprehension page) must deploy or production silently skips comprehension (see the teacher-only-report bullet). Working tree also holds the selection-color, timed-read, and doc changes. Separately, existing sessions' stored `error_patterns` need a re-run of `scripts/backfill-patterns.ts` (relax its skip filter) to drop the pre-0019 inflated tail.

**Known gaps / tech debt:**
- **WER validation still has zero samples** (`validation-data/` holds only the template). Roadmap marks this as blocking any demo; the script is ready.
- Dashboard not seeded with realistic demo sessions (roadmap pre-demo item).
- No automated tests — quality relies on `lint` + `build` + manual testing. `lib/scoring/` remains the highest-value target for unit tests.
- Rate limiting on `/api/score` is per-Vercel-instance (in-memory) — bounds bursts, not exact; fine pre-pilot, revisit with a shared store if abuse appears.
- `.env.local`'s `SUPABASE_ACCESS_TOKEN` is expired (management API returns 401) — refresh it or remove it.
- `prep-files/ARCHITECTURE.md` is the original April plan, outrun by the code. Treat as rationale, not current state.
- Strategy fields (vision, pricing, GTM specifics) — see TODOs in `docs/PRODUCT.md`.

**Next concrete steps:**
- Commit the 2026-07-10 working-tree changes.
- **WER validation:** record 5–10 diverse readings (AAVE, Spanglish, newcomer accents), hand-transcribe, run `scripts/validate-wer.ts`. Target ±5 WCPM. Blocks the Eileen demo.
- Seed the dashboard with realistic sessions across benchmark bands before the demo.
- Remaining compliance items for ERMA: vendor disclosure one-pager, data-retention policy/automatic deletion, Rasinski MDFS tooltip rewrites.
- Decide on a testing strategy for `lib/scoring/`.
